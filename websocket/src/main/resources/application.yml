spring:
  application:
    name: websocket
  mongodb:
    uri: mongodb://saketh:saketh@localhost:27017/chat_app?authSource=admin
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

server:
  port: 8088

app:
  auth:
    cookie-secure: false
  upload:
    dir: uploads
